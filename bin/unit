#!/bin/bash
set -e
cd "$(dirname "$0")"/..

set -x
docker run --rm \
    -v "$(pwd)/yamltools:/src/yamltools" \
    --workdir=/src \
    ${DOCKER_IMAGE} \
    nosetests -v --exclude="integration" ${@:2}
